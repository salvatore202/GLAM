# URL base del backend GLAM (Boundary.web.ApiServer).
# In sviluppo locale, con il backend avviato su localhost:8080, va bene il default.
# Se il backend gira altrove (un altro host, un tunnel, un container), aggiorna questo valore.
VITE_API_BASE_URL=http://localhost:8080/api
